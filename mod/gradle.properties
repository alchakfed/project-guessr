# Pinned versions. Bump these once, everything downstream reads from here.
# Confirm the exact yarn/fabric_version/fabric_api builds on first run against
# https://fabricmc.net/develop (pick the 1.21.11 entries).

org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Minecraft
minecraft_version=1.21.11
yarn_mappings=1.21.11+build.1
loader_version=0.17.2

# Fabric API
fabric_version=0.135.1+1.21.11

# Mod metadata
mod_version=1.0.0
maven_group=com.projectguessr
archives_base_name=projectguessr
