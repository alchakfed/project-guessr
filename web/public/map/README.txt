# This folder is filled by tools/build-map.js:
#   map.png        the stitched JourneyMap image
#   map-meta.json  world<->pixel bounds used by the guess map
#
# It's here so web/server.js has a stable path to serve. Empty until you run
# the tools (see the top-level README, Step 3).
